'use client';

interface SliderProps {
    slides: {
        url: string;
        label: string;
        subtitle: string;
    }[];
}

const Slider: React.FC<SliderProps> = ({slides}) => {
  return (
    <div>
      
    </div>
  )
}

export default Slider
